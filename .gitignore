# --- ROM and anything derived from it. Never commit these. ---
rom/
*.nds
*.NDS
*.srl
extract/
out/
.cache/
*.nsbmd
*.nsbtx
*.nsbca
*.sdat
*.sseq
*.sbnk
*.swar
*.bin.dump

# --- build / tooling ---
node_modules/
dist/
.vite/
coverage/
*.tsbuildinfo
.DS_Store

# The slice plan names the title in its filename and throughout its text, which
# the codename rule in CLAUDE.md does not allow in this repository. Kept local
# until it is renamed and rewritten against the codename.
dq9-slice1-angel-falls.md
